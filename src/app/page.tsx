export default function Home() {
  return (
    <main className="entry">
      <p className="eyebrow">Agent Builder / prompt to verified package</p>
      <h1>Describe the agent you need.</h1>
      <p>
        Start with one outcome. Agent Builder turns it into an inspectable specification and
        sends accepted designs to HarnessBuilder for verification.
      </p>
    </main>
  );
}
